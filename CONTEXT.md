# GitHub 原生 Markdown 增强

为用户正在使用的 GitHub 编辑器提供 Markdown 插入入口，保留原有编辑体验。

## Language

**原生编辑器**：GitHub 页面原本提供的正文编辑区域及其编辑、预览能力。扩展不成为正文的另一份编辑来源。

**编辑器实例**：页面中一块可独立编辑的正文区域。一个 Issue 表单可以包含多个实例。

**增强入口**：与一个编辑器实例对应的“增强”下拉按钮。入口中的操作只作用于这个实例。

**插入命令**：用户主动选择的一种 Markdown 操作，例如插入提示块，或将选中文本包成折叠块。

**提示块**：GitHub 的 Alert，包含 NOTE、TIP、IMPORTANT、WARNING、CAUTION 五种类型。

**选区快照**：打开增强菜单时，目标编辑器的插入位置或选中文字范围。它只对当时的编辑内容有效。
